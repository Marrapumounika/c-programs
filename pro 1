#include<stdio.h>
main()
{
	int n,k,i,se=0,so=0;
	printf("enter n & k");
	scanf("%d%d",&n,&k);
	for(i=1;i<=n;i++)
	if(i%k==0)
	{
		printf("%d\n",i);
		if(i%2==0)
		se=se+i;
		else
		so=so+i;
	}
	printf("%d %d\n",se,so);
}
